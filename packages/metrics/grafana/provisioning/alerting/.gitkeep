Grafana reads alerting provisioning files from this directory. Mounting the
provisioning tree replaces Grafana's built-in one, so this must exist even when
empty or Grafana logs an error on every startup.
